"""exp95e_full_114_consensus_universal — universal scaling of F53.

Public submodules:
    _enumerate  — variant generators for V1 / SHORT / FULL / SAMPLE scopes
    _detector   — deployable is_quranic() forgery tool
    _audit      — drift / fingerprint / clustering helpers
    run         — main pipeline (callable as `python -m`)
"""
